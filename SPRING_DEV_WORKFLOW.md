# Spring Boot 開発ワークフロー

このドキュメントでは、Spring Bootアプリケーションの開発中に、コード変更を効率的に反映するための方法を説明します。

## Spring Boot DevToolsについて

このプロジェクトでは、Spring Boot DevToolsが設定されています。DevToolsは、開発中にコード変更を自動的に検出し、アプリケーションを再起動することで、変更を即時に反映します。

## 効率的な開発ワークフロー

### 1. アプリケーションの起動

アプリケーションを起動するには、以下のコマンドを使用します：

```bash
./gradlew bootRun
```

このコマンドは、DevToolsが有効な状態でアプリケーションを起動します。

### 2. コード変更の反映

コードを変更した後、以下のいずれかの方法で変更を反映できます：

#### 方法1: `.reloadtrigger`ファイルの更新

プロジェクトに含まれる`reload-spring.sh`スクリプトを実行することで、`.reloadtrigger`ファイルを更新し、アプリケーションの再起動をトリガーできます：

```bash
./reload-spring.sh
```

このスクリプトは、`.reloadtrigger`ファイルの内容を更新するだけで、アプリケーションの再起動をトリガーします。

#### 方法2: VSCodeの自動ビルド機能の利用

VSCodeの自動ビルド機能が有効になっているため、Javaファイルを保存すると自動的にコンパイルされます。コンパイルが完了すると、DevToolsがクラスの変更を検出し、アプリケーションを再起動します。

### 3. 注意点

- 一部の変更（例：設定ファイルの変更）は、自動的に反映されない場合があります。その場合は、`reload-spring.sh`スクリプトを実行するか、アプリケーションを手動で再起動してください。
- 大規模な変更や依存関係の追加など、一部の変更は`./gradlew clean build`を実行する必要がある場合があります。

## まとめ

Spring Boot DevToolsと`.reloadtrigger`ファイルを使用することで、多くの場合、`./gradlew clean build`を実行せずにコード変更を反映できます。開発中は`./gradlew bootRun`でアプリケーションを起動し、コード変更後は`./reload-spring.sh`スクリプトを実行するか、VSCodeの自動ビルド機能に任せることで、効率的な開発が可能になります。
