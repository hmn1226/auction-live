# 自動ビルドガイド

このドキュメントでは、Spring Bootアプリケーションの開発中に、コード変更を自動的に反映するための方法を説明します。

## 問題

Spring Boot DevToolsを設定しても、コード変更が自動的に反映されず、`./gradlew clean build`を実行する必要がある場合があります。これは開発効率を低下させる原因となります。

## 解決策

以下の2つの方法を提供します。それぞれの状況に応じて最適な方法を選択してください。

### 方法1: watch-and-build.sh スクリプト（推奨）

このスクリプトは、`fswatch`コマンドを使用して、ファイル変更をリアルタイムで監視し、変更があった場合に自動的に`./gradlew clean build`を実行します。

#### 前提条件

`fswatch`コマンドをインストールする必要があります：

```bash
brew install fswatch
```

#### 使用方法

```bash
./watch-and-build.sh
```

このスクリプトを実行すると、`src/main/java`と`src/main/resources`ディレクトリ内のファイル変更をリアルタイムで監視し、変更があった場合に自動的に`./gradlew clean build`を実行します。

#### 特徴

- リアルタイムでファイル変更を検出
- 効率的なファイル監視
- 複数のディレクトリを監視可能

### 方法3: Gradleの継続的なビルドタスク

Gradleの`--continuous`フラグを使用して、ファイル変更を監視し、変更があった場合に自動的にビルドを実行します。

#### 使用方法

```bash
./gradlew continuousBuild --continuous
```

このコマンドを実行すると、Gradleがファイル変更を監視し、変更があった場合に自動的にビルドを実行します。

#### 特徴

- Gradleの標準機能を使用
- プロジェクト全体のファイル変更を監視
- ビルドプロセスとの統合が容易

## 推奨ワークフロー

1. アプリケーションを起動する（別のターミナルで）：
   ```bash
   ./gradlew bootRun
   ```

2. 自動ビルドを開始する（別のターミナルで）：
   ```bash
   ./watch-and-build.sh  # 推奨
   ```
   または
   ```bash
   ./gradlew continuousBuild --continuous
   ```

3. コードを変更する
4. 自動的にビルドが実行され、変更が反映される

## 注意点

- これらの方法は、`./gradlew clean build`を自動的に実行するため、ビルド時間がかかる場合があります。
- 大規模なプロジェクトでは、ビルド時間が長くなる可能性があります。
- 一部の変更（例：設定ファイルの変更）は、アプリケーションの再起動が必要な場合があります。
